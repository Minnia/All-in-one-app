import styled from 'styled-components';
import {Text} from 'react-native';

export const EvenTodo = styled(Text)`
  background-color: blue;
`;

export const OddTodo = styled(Text)`
  background-color: grey;
`;
